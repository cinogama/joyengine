import rscene.std;
import je.gui;

func Editor_ScriptEditor()
{
    using je::gui;
    var open = true;
    Begin("RScene脚本", WindowsAttribute::ImGuiWindowFlags_NoResize, ref open);

    static var textbuf = TextBuffer("");
    if (InputTextMultiline("##", textbuf, 500, 300))
    {
        std::println(textbuf->get());
    }

    End();
    return !open;
}