import woo.std;
import je.gui;

namespace generic
{
    func MsgBox(title:string, shown:string)
    {
        using je::gui;

        let open = true;

        Begin(F"{title}##{JobID()}"
                    , WindowsAttribute::ImGuiWindowFlags_AlwaysAutoResize);
        {
            Text(shown);
            if (Button("确定"))
                open = false;
        }
        End();

        return !open;
    }
}