import woo.std;
import je.gui;

func Editor_ScriptEditor(ref open:bool)
{
    using je::gui;
    if(!open) return !open;

    Begin("Woolang脚本", WindowsAttribute::ImGuiWindowFlags_NoResize, ref open);

    static let textbuf = TextBuffer("");
    if (InputTextMultiline("##", textbuf, 500.0, 300.0))
    {
        std::println(textbuf->get());
    }

    End();
    return !open;
}