import woo.std;
import woo.thread;
import je;
import builtin.api.welcome.demo;
import builtin.api.universe.context;

jeapi::welcome::display_copyright_msg();

jeapi::universe::startup(func(universe){
	
	// 创建一个默认世界用于渲染
	let w = universe->je::world::create;
	w->add_system(je::typeinfo::load_from_name("Graphic::DefaultGraphicPipelineSystem")->val);

	// TODO: Universe 就是全部环境，加载项目就直接干掉这个Universe，重新搞一个新的

});