/*
Practice: 对实体的一切改动均通过 Practice 对象执行落实。

Practice 是对实体的编辑管理，保留编辑操作的历史，并支持回退功能。
*/

import je::ecs;
import je::internal;

import pkg::interface;

using je;

namespace JEEditor::Editor
{
    alias EuidT = entity::internal::euid_t;
    
    using Practice = struct{}
    {
        interface! IAction
        {
            func redo(self)=> bool;
            func undo(self)=> bool;
        }

        using CreateEntity = struct{}
        {
            func create()=> CreateEntity
                where require!(CreateEntity: IAction);
            {   
                return CreateEntity{};
            }
            func redo(_: CreateEntity)=> bool
            {
                return false;
            }
            func undo(_: CreateEntity)=> bool
            {
                return false;   
            }
        }
    }
}
