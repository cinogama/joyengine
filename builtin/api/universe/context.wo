// builtin.api.universe.context
// 用于简单管理引擎上下文的简单工具

namespace jeapi::universe
{
	public func startup(callback: (je::universe)=>void)
	{
		let new_universe = je::universe::create();

		callback(new_universe);

		new_universe->wait;
		new_universe->close;
	}
}