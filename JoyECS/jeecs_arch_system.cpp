#define JE_IMPL
#include "jeecs.hpp"

namespace jeecs_impl
{
    class arch_manager
    {
        jeecs::arch::arch_type* find_or_add_arch(const jeecs::arch::types_set& _types)
        {
            return nullptr;
        }
    };
}